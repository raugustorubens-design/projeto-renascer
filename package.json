// CHECK 1 — Importações
import { PageTransition } from "@/components/layout/PageTransition";

// CHECK 2 — Componente principal
export default function HomePage() {
  // CHECK 2.1 — Retorno visual
  return (
    <PageTransition>
      {/* CHECK 2.2 — Estrutura da página */}
      <section>
        <div>
          <h1>Renascer</h1>
        </div>
      </section>
    </PageTransition>
  );
}
